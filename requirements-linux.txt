# Kinder Vision — Linux/Ubuntu draft requirements for OpenClaw runtime
#
# Notes:
# - This file is a deployment draft for Linux.
# - Keep versions close to verified set, but add OS/Python markers for better wheel compatibility.
# - For GPU servers, install torch/torchvision from the correct CUDA index URL before/after this file.

# NOTE: mediapipe 0.10.18 (latest aarch64) requires numpy<2, so we cap numpy here
# and use the last opencv-python-headless that supports numpy 1.x.
numpy>=1.26,<2
scipy==1.17.1
pandas==3.0.2
opencv-python-headless==4.10.0.84
librosa==0.11.0
soundfile==0.13.1
matplotlib==3.10.9
tqdm==4.67.3
lap==0.5.13
ultralytics==8.4.46

# PyTorch:
# - CPU default on Linux (adjust for CUDA as needed).
torch==2.11.0; platform_system == "Linux"
torchvision==0.26.0; platform_system == "Linux"

# MediaPipe:
# - Linux wheels are generally more stable on CPython 3.10-3.12.
# - If OpenClaw runtime uses 3.13+, this may require a fallback strategy.
mediapipe==0.10.18; platform_system == "Linux" and python_version >= "3.10" and python_version < "3.13"
